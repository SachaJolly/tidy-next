import { http, HttpResponse } from 'msw';

export const mswHandlers = {
  // Add mock endpoints as needed by your stories
  // Example:
  // lists: [
  //   http.get('/api/lists', () =>
  //     HttpResponse.json({ items: [] })
  //   ),
  // ],
};
